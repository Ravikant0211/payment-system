apiVersion: 1

providers:
  - name: Payment System
    orgId: 1
    folder: Payment System
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
